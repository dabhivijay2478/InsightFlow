- Connector card components (e.g. `ConnectorCard`) must NOT re-apply wave/availability filtering locally. The catalog (`ConnectorCatalog`) is the single source of truth — it already accounts for `wave`, `ENABLED_CONNECTOR_IDS`, runtime capability, and role support. Re-implementing any of those gates in the card produces double-locking where a connector passes the catalog but is incorrectly marked "Coming Soon" / locked in the UI. Trust the `isComingSoon` prop exactly as the catalog passes it. Confidence: 0.85
- When a connector supports both API key and OAuth authentication, and the older method is deprecated or being phased out (e.g., Zendesk API tokens deprecated April 2027), implement only the OAuth flow and remove the deprecated API key flow entirely. Do not maintain parallel authentication paths — consolidate to the forward-looking method. Confidence: 0.80
- When a connector is "in the available list but the new-connection form says not found / not available", the bug is usually another local `wave > 1` or hard-coded wave gate in that screen — not a backend issue. Search every connector-related screen/component for `wave > 1`, `wave === 1`, or `wave: 1` guards in the same change as the catalog fix. `new-connection-form-screen.tsx`, `connection-detail-screen.tsx`, `edit-connection-screen.tsx`, and any sibling connection route are common offenders. Confidence: 0.85# Connector Rollout

# runtime-gating
- New connectors (Linear, Notion, Asana, Stripe, HubSpot, Airtable, MySQL, ClickHouse) must be runtime health-gated: the frontend `/api/v1/connectors/health` endpoint reports per-connector source/destination availability, and the UI hides the connector card when the capability is unavailable. Confidence: 0.85
- When a connector is added but not showing in the UI, check that the runtime enablement allowlist (e.g., `ENABLED_CONNECTOR_IDS`) includes the connector — having it in the registry is not enough; the gate is explicit. Confidence: 0.80
- Frontend connector catalog entries must declare `availability: "runtime"` (not omit the field) when they need ELT-side health confirmation. An omitted `availability` field renders the connector even when the backend can't support it. The matching ELT `/health` endpoint must publish a connector entry with `{source, destination, available, reason}` so the frontend gate has something to read. Confidence: 0.85
- The `ConnectorCatalog` "Available Now" vs "Coming Soon" split must be driven purely by the availability gate (role, `ENABLED_CONNECTOR_IDS`, runtime capability), NOT by `connector.wave`. Hard-coding `wave === 1` into the available filter pushes all wave-2+ connectors into "Coming Soon" regardless of whether they're actually ready. The wave field is metadata, not a gate. Confidence: 0.85
- The Go `ConnectorsHealth` degraded-fallback map (used when ELT `/health` is unreachable) must include every connector in the registry, not just a hand-picked subset. A missing entry in the fallback will silently hide that connector from the UI whenever ELT is down. Whenever adding a new connector to `ENABLED_CONNECTOR_IDS`, audit the Go fallback map in the same change. Confidence: 0.85
- Connector card components (e.g. `ConnectorCard`) must NOT re-apply wave/availability filtering locally. The catalog (`ConnectorCatalog`) is the single source of truth — it already accounts for `wave`, `ENABLED_CONNECTOR_IDS`, runtime capability, and role support. Re-implementing any of those gates in the card produces double-locking where a connector passes the catalog but is incorrectly marked "Coming Soon" / locked in the UI. Trust the `isComingSoon` prop exactly as the catalog passes it. Confidence: 0.85
</content>
</invoke>